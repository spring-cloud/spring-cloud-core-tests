spring:
  application:
    name: zuulConfigDiscoveryTest
  cloud:
    config:
      username: user
      password: password
      failFast: true
      discovery:
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://user::password@localhost:8761/eureka/
