server:
  port: 9001
spring:
  application:
    name: eurekafirst
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: configserver
eureka:
  instance:
    leaseRenewalIntervalInSeconds: 10
  client:
    registryFetchIntervalSeconds: 5
  